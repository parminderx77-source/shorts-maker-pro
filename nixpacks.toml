[phases.setup]
nixPkgs = ['python311', 'ffmpeg', 'gcc']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[start]
cmd = 'gunicorn main:app --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT --timeout 600'
